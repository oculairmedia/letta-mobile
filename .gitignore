# OS
.DS_Store

# Android / IntelliJ
build/
.idea
.gradle
local.properties
*.iml
*.hprof
.cxx/
*.keystore
!debug.keystore
*.jks
keystore.properties

# Kotzilla — contains API key, per-developer, never committed
kotzilla.json

# OpenCode / IDE
.vscode
.opencode/

# Claude Code worktrees (accidentally committed as gitlinks; submodule init fails)
.claude/worktrees/

# Stale submodule references (committed as gitlinks without .gitmodules entries)
.compose-performance-skills-source
.skills

# Beads / Dolt
.dolt/
*.db
.beads-credential-key

# Orphan KMP shared-ui build output (module is not in settings.gradle.kts)
android-compose/shared-ui/

# Kotlin compiler error logs
android-compose/.kotlin/
android-compose/native/mermaid_renderer/target/

# Letta local config
.letta/

# Project-local MCP/code-intelligence caches and advisory artifacts
.local/
.serena/project.local.yml
.serena/cache/
.serena/logs/
.serena/memories/
index.scip
*.scip

# Generated reports
LETTA_ADMIN_API_REPORT.md
reports/*.md
reports/*.json

# Local performance traces
scripts/perf/traces/*
!scripts/perf/traces/.gitignore

# frame-shape-capture corpus: raw diagnostic capture data (may contain live
# conversation content), not source. Kept off git; see
# tools/frame-shape-capture/README.md.
tools/frame-shape-capture/corpus/*
!tools/frame-shape-capture/corpus/.gitkeep
**/__pycache__/
*.py[cod]
models-cache/
android-compose/app/libs/embedded-git/
android-compose/app/libs/embedded-bash/

android-compose/app/libs/embedded-curl/
android-compose/app/libs/embedded-node-cli/
android-compose/local.properties
.tools/
