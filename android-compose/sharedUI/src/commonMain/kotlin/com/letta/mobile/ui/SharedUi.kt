package com.letta.mobile.ui

/**
 * Marker for the `:sharedUI` KMP module (Phase 3a scaffold).
 *
 * Shared Compose sources move here from `:sharedLogic` in Phase 3b
 * (`composeUi` + `jvmAndAndroid` UI packages). Until then this type exists
 * so the module has a compileable commonMain entry point.
 */
object SharedUi {
    const val MODULE = "sharedUI"
}
