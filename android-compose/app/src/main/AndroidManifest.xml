<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />
    <uses-permission android:name="android.permission.POST_NOTIFICATIONS" />
    <uses-permission android:name="android.permission.RECEIVE_BOOT_COMPLETED" />
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE" />
    <uses-permission android:name="android.permission.REQUEST_IGNORE_BATTERY_OPTIMIZATIONS" />
    <uses-permission android:name="android.permission.VIBRATE" />
    <!--
      letta-mobile-50p2: switched from FOREGROUND_SERVICE_DATA_SYNC to
      FOREGROUND_SERVICE_REMOTE_MESSAGING. The dataSync FGS type has a
      6-hour rolling daily quota on Android 14+ — once exhausted any
      startForeground() call throws ForegroundServiceStartNotAllowedException
      and crashes the process loop. ChatPushService is fundamentally a chat
      push channel (websocket + notification publisher), so remoteMessaging
      is both the correct semantic and quota-exempt.
    -->
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE_REMOTE_MESSAGING" />

    <!-- letta-mobile-rl0d (audio): hold-to-dictate uses on-device speech
         recognition (android.speech.SpeechRecognizer). RECORD_AUDIO is
         requested at runtime by HoldToDictateButton on first tap. The
         <queries> block lets SpeechRecognizer enumerate installed
         recognition services on Android 11+ where package visibility
         is restricted by default. -->
    <uses-permission android:name="android.permission.RECORD_AUDIO" />
    <queries>
        <intent>
            <action android:name="android.speech.RecognitionService" />
        </intent>
    </queries>

    <!-- Provider read CRUD (capability epic riq2i P3): runtime-gated normal
         dangerous permissions for reading contacts and calendar data. -->
    <uses-permission android:name="android.permission.READ_CONTACTS" />
    <uses-permission android:name="android.permission.READ_CALENDAR" />

    <application
        android:name=".LettaApplication"
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:networkSecurityConfig="@xml/network_security_config"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.LettaMobile">

        <!--
          LiteRT GPU delegate (letta-mobile-37uvo): on Android 12+ vendor
          native libraries must be declared or the linker namespace blocks
          them — without these, OpenCL fails to dlopen (libvndksupport.so)
          and GPU inference cannot initialize. libOpenCL-pixel.so is the
          Pixel/Tensor variant. required=false keeps installs working on
          devices without the libs.
        -->
        <uses-native-library android:name="libOpenCL.so" android:required="false" />
        <uses-native-library android:name="libOpenCL-car.so" android:required="false" />
        <uses-native-library android:name="libOpenCL-pixel.so" android:required="false" />

        <!--
          Sentry configuration.

          The SDK is initialized programmatically in `SentryInitializer`
          (via AndroidX Startup). We disable auto-init because
          `io.sentry.traces.sample-rate` cannot be expressed as a
          numeric manifest value — `ManifestMetadataReader.readDouble`
          only reads `Bundle.getFloat`/`getInt`, so a `@string/...`
          reference falls through to the -1 default and sampling is
          silently disabled (see letta-mobile-o7ob.7).
        -->
        <meta-data
            android:name="io.sentry.auto-init"
            android:value="false" />

        <provider
            android:name="androidx.startup.InitializationProvider"
            android:authorities="${applicationId}.androidx-startup"
            android:exported="false"
            tools:node="merge">
            <meta-data
                android:name="com.letta.mobile.crash.SentryInitializer"
                android:value="androidx.startup" />
        </provider>

        <activity
            android:name=".MainActivity"
            android:exported="true"
            android:launchMode="singleTop"
            android:windowSoftInputMode="adjustNothing"
            android:theme="@style/Theme.LettaMobile.Starting">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
            <intent-filter>
                <action android:name="android.intent.action.VIEW" />
                <category android:name="android.intent.category.DEFAULT" />
                <category android:name="android.intent.category.BROWSABLE" />
                <data
                    android:scheme="letta"
                    android:host="agent" />
            </intent-filter>
            <intent-filter>
                <action android:name="android.intent.action.SEND" />
                <category android:name="android.intent.category.DEFAULT" />
                <data android:mimeType="text/*" />
            </intent-filter>
        </activity>

        <receiver
            android:name=".channel.ChatPushAlarmReceiver"
            android:enabled="true"
            android:exported="false" />

        <service
            android:name=".channel.ChatPushService"
            android:exported="false"
            android:foregroundServiceType="remoteMessaging" />

        <service
            android:name=".runtime.local.LocalLettaCodeService"
            android:exported="false"
            android:foregroundServiceType="remoteMessaging" />

        <!--
            Notification listener for the notification.poll tool
            (feat/mobile-notification-tool). OPT-IN ONLY: inert until the user
            grants "Notification access" in Settings; the tool reports
            SettingsRequired until then.
        -->
        <service
            android:name=".runtime.notifications.LettaNotificationListenerService"
            android:exported="false"
            android:permission="android.permission.BIND_NOTIFICATION_LISTENER_SERVICE">
            <intent-filter>
                <action android:name="android.service.notification.NotificationListenerService" />
            </intent-filter>
        </service>

    </application>

</manifest>
