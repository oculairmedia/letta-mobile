# System 1 (Laya) service dependencies.
#
# torch is intentionally NOT pinned here: the right wheel depends on the host's
# CUDA toolkit (or lack of one). Install it first from pytorch.org, then this
# file. Verified against torch 2.7.1+cu126.
laya>=0.1.7
fastapi>=0.115
uvicorn>=0.30
pydantic>=2.7
httpx>=0.27
